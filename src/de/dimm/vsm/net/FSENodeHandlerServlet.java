/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package de.dimm.vsm.net;

/**
 *
 * @author Administrator
 */

public class FSENodeHandlerServlet
{
/*    HashMap<Long, FileSystemElemNodeHandler> handlerMap;
    StoragePoolHandler poolHandler;

    public FSENodeHandlerServlet(StoragePoolHandler ph)
    {
        poolHandler = ph;
        handlerMap = new HashMap<Long, FileSystemElemNodeHandler>();
    }

    void addHandler( FileSystemElemNode pool )
    {
        if (handlerMap.containsKey(pool.getIdx()))
        {
            handlerMap.remove(pool.getIdx());
        }
        FileSystemElemNodeHandler handler = new FileSystemElemNodeHandler(pool, poolHandler);
        handlerMap.put(pool.getIdx(), handler);
    }

    FileSystemElemNodeHandler getHandler( FileSystemElemNode pool )
    {
        FileSystemElemNodeHandler handler = handlerMap.get(pool.getIdx());
        if (handler == null)
        {
            handler = new FileSystemElemNodeHandler(pool, poolHandler);
            handlerMap.put(pool.getIdx(), handler);
        }
        return handler;
    }
*/


}
